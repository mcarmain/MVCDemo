@model MVCDemo.Models.Item

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 class="modal-title">Edit Item</h3>
</div>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Id)

    <div class="modal-body">

        <div class="form-horizontal">
            <div class="form-group">
                @Html.LabelFor(m => Model.Name, new { @class = "control-label col-sm-3" })
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control required" })
                    <div>
                        @Html.ValidationMessageFor(m => m.Name)
                    </div>
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => Model.Description, new { @class = "control-label col-sm-3" })
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Description, new { @class = "form-control required" })
                    <div>
                        @Html.ValidationMessageFor(m => m.Description)
                    </div>
                </div>
            </div>

            @*<div class="form-group">
                @Html.LabelFor(model => model.ItemTypeId, null, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-sm-9">
                    @Html.DropDownList("ItemTypeId", null, htmlAttributes: new { @class = "form-control" })
                    @Html.ActionLink("Create new type", "CreateType", new { id = Model.Id })
                    @Html.ValidationMessageFor(model => model.ItemTypeId, "", new { @class = "text-danger" })
                </div>
            </div>*@
           
        </div>
    </div>

    <div class="modal-footer">
        <span id="progress" class="text-center" style="display: none;">
            <img src="/images/wait.gif" alt="wiat" />
            Wait..
        </span>

        <input class="btn btn-primary" type="submit" value="Save" />
        <button class="btn btn-warning" data-dismiss="modal">Close</button>
    </div>
}
<script>
    $("form").removeData("validator");
    $("form").removeData("unobtrusiveValidation");
    $.validator.unobtrusive.parse("form");
</script>

